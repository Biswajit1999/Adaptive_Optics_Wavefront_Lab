# Changelog

## 3.0.0 — 2026-09-20

- Added an exact FITS-to-browser provenance audit covering all retained values.
- Added a predeclared distributional and temporal-fidelity study of stride-50
  sampling; the distribution gate passes and the temporal gate fails.
- Published deterministic JSON, CSV, and SVG evidence plus an explicit claim
  ledger and methods protocol.
- Extracted a directly tested six-mode science core and made the deployed worker
  import it, eliminating test-only numerical implementations.
- Added fail-closed telemetry schema checks and direct PID/Maréchal/FFT contracts.
- Replaced stale branch-specific checks with one pinned clean-checkout workflow.
- Added public evidence and research-maturity panels with explicit non-claims.

