# Upgrade v2 scope

The Point Spread Function panel now derives its intensity from a phase-only circular pupil using a 2D discrete Fourier transform. This replaces the prior qualitative radial-gradient sketch.

The dashboard still reports its compact residual error budget separately. Those scalar fitting, servo-lag, and sensor-noise terms are not represented as a physical time-varying phase screen in the displayed pupil.
